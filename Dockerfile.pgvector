FROM postgres:16

RUN set -eux; \
    apt-get update; \
    apt-get install -y --no-install-recommends postgresql-16-pgvector; \
    rm -rf /var/lib/apt/lists/*

# Erstellt die Extension bei der Initialisierung automatisch.
COPY docker/init-pgvector.sql /docker-entrypoint-initdb.d/10_pgvector.sql
